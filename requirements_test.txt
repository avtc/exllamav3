# Test requirements for P2P backend functionality

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-xdist>=3.3.0
pytest-benchmark>=4.0.0
pytest-mock>=3.11.1
pytest-asyncio>=0.21.1

# Mocking and utilities
mock>=5.1.0
freezegun>=1.2.2

# Performance and profiling
psutil>=5.9.0
memory-profiler>=0.60.0
py-spy>=0.3.0

# CUDA and GPU testing (if available)
# These are optional but recommended for full P2P testing
cupy-cuda11x>=12.0.0  # CUDA 11.x support
# cupy-cuda12x>=12.0.0  # CUDA 12.x support - uncomment if using CUDA 12

# Optional: NCCL testing (for comparison testing)
# torch[extra] typically includes NCCL if available

# Development and linting
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# Documentation
sphinx>=7.0.0
sphinx-rtd-theme>=1.3.0

# Additional testing utilities
coverage[toml]>=7.2.0
pre-commit>=3.3.0
tox>=4.0.0